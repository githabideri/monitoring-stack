apiVersion: 1
datasources:
  - name: {{ grafana_prometheus_name }}
    uid: prometheus-homelab
    type: prometheus
    access: proxy
    url: {{ grafana_prometheus_url }}
    isDefault: true
    editable: false
