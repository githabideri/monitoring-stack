[Unit]
Description=Prometheus TSDB backup (daily)

[Timer]
# 23:30 — adjust to your backup-destination availability window.
OnCalendar=*-*-* 23:30:00
Persistent=true
RandomizedDelaySec=15
AccuracySec=1m

[Install]
WantedBy=timers.target
